version: '3.8'

services:
  web:
    build: .
    container_name: honkai-star-rail-app
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "description=Honkai Star Rail Character Randomizer"

